import React from 'react';

const BackgroundImageTest = props => {
  return(
    <p className="background-image-test">If you see an image, Webpack has been able to copy the SCSS background-image asset and resolve the URL</p>
  );
}

export default BackgroundImageTest;